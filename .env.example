# Example environment for local development.
#
# - Do NOT commit your real `.env` (it is ignored by git).
# - Modal runs do NOT automatically read `.env`; use `modal secret create ...`
#   to inject secrets into the sandbox.
#
# Required for all training/sampling via the Tinker MCP server:
TINKER_API_KEY=tml-...
#
# Required for running the default agent (Claude Code) in Modal:
ANTHROPIC_API_KEY=sk-ant-...
#
# Required for running the Codex agent in Modal:
OPENAI_API_KEY=sk-...
#
# Optional (recommended): Weights & Biases observability:
WANDB_API_KEY=wandb_...
WANDB_PROJECT=tinkerer
# WANDB_ENTITY=your-wandb-entity
# WANDB_GROUP=your-wandb-group
#
# Optional: Hugging Face token for gated datasets:
HF_TOKEN=hf_...
#
# Optional: Codex model/runtime overrides (set in the Modal env/secret):
# CODEX_MODEL=gpt-5.2-codex
# CODEX_REASONING_EFFORT=xhigh
# CODEX_REASONING_SUMMARY=detailed
#
# Optional: harness debug/observability toggles:
TINKERER_DEBUG=1
# TINKERER_GRPO_SAMPLING_DEBUG_PROMPT_LIMIT=3  # -1 for all prompts, 0 disables
# TINKERER_OUTPUT_CAPTURE_MB=200
